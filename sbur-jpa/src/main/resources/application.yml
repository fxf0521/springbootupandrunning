spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springbootupandrunning
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
#  sql:
#    init:
#      mode: always
#      schema-locations: classpath*:schema-mysql.sql
#      data-locations: classpath*:data-mysql.sql
